SPR_civil_war = {
 	SPR_surrender = {
 		icon = spr_the_inevitable_civil_war
 		fire_only_once = no
 		allowed = { tag = SPR }
 		available = {date < 1939.6.1}
 		visible={has_war=yes}
 		cost = 0
		days_remove = 14
 		remove_effect = {
			set_global_flag = Axis_lost_civil_war
			SPR = {remove_ideas = SPA_civil_war}
			SOV = {add_ideas = SOV_lessons_of_spain_idea}
 			country_event = spain.11
 		}
 	}
	SPR_send_troops = {
		activation = {
			always=no
		}
		icon = generic_operation
		available = {
			always=no
		}
		allowed = {tag = SPR}
		days_mission_timeout=60
		visible = {has_war=yes}
		is_good=no
		fire_only_once = yes
		timeout_effect  = {
			GER = {set_rule = {can_send_volunteers  = no}}
			ITA = {set_rule = {can_send_volunteers  = no}}
			HUN = {set_rule = {can_send_volunteers  = no}}
			ROM = {set_rule = {can_send_volunteers  = no}}
			BUL = {set_rule = {can_send_volunteers  = no}}
			JAP = {set_rule = {can_send_volunteers  = no}}
			MAN = {set_rule = {can_send_volunteers  = no}}
			ENG = {set_rule = {can_send_volunteers  = no}}
			FRA = {set_rule = {can_send_volunteers  = no}}
			CAN = {set_rule = {can_send_volunteers  = no}}
			SAF = {set_rule = {can_send_volunteers  = no}}
			RAJ = {set_rule = {can_send_volunteers  = no}}
			AST = {set_rule = {can_send_volunteers  = no}}
			NZL = {set_rule = {can_send_volunteers  = no}}
			SOV = {set_rule = {can_send_volunteers  = no}}
		}
	}	
}

SPN_civil_war = {	
 	SPN_surrender = {
 		icon = spr_the_inevitable_civil_war
 		fire_only_once = no
 		allowed = { tag = SPN }
 		available = {date < 1939.6.1}
 		visible={has_war=yes}
 		cost = 0
		days_remove = 14
 		remove_effect = {
			set_global_flag = Allies_lost_civil_war
			SPR = {add_timed_idea = {idea= SPA_recovering_from_civil_war days = 700}}
			SPR = {remove_ideas = SPA_civil_war}
			GER = {add_ideas = GER_lessons_of_spain_idea}
			country_event = spain.11
 		}
 	}
}